package molecule.core.boilerplate

import molecule.core.boilerplate.base._
import scala.language.higherKinds


/** Type distribution to groups of functionality for 'output'/standard molecules. */
object outIndex {

  trait OutIndex_0
    extends NS00[Nothing]

  trait OutIndex_1[A]
    extends NS01[A]

  trait OutIndex_2[A, B]
    extends NS02[A, B]

  trait OutIndex_3[A, B, C]
    extends NS03[A, B, C]

  trait OutIndex_4[A, B, C, D]
    extends NS04[A, B, C, D]

  trait OutIndex_5[A, B, C, D, E]
    extends NS05[A, B, C, D, E]

  trait OutIndex_6[A, B, C, D, E, F]
    extends NS06[A, B, C, D, E, F]

  trait OutIndex_7[A, B, C, D, E, F, G]
    extends NS07[A, B, C, D, E, F, G]

  trait OutIndex_8[A, B, C, D, E, F, G, H]
    extends NS08[A, B, C, D, E, F, G, H]
}